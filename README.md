This is just a sample project to try Specflow without Visual Studio

The project setup was performed be executing:

* dotnet new nunit
* dotnet add package Specflow.NUnit
* dotnet add package SpecFlow.Tools.MsBuild.Generation
